# PA0 - Ubuntu and GitHub Setup

For PA0, I set up the required Ubuntu 24.04 environment using VirtualBox on Windows 11.

I verified:
- Ubuntu 24.04
- x86_64 architecture
- 4 CPU cores, 8 GB RAM, and sufficient disk space
- sudo and dialout group membership
- network and DNS connectivity
- USB passthrough through VirtualBox
- Git installation and configuration

The verification report is `zhang.zhiqi2.txt`, and `desktop.png` shows the completed verification run.
